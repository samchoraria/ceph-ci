roles:
- [client.0]

overrides:
  ceph:
    log-whitelist:
    - but it is still running
    - overall HEALTH_
    - \(POOL_APP_NOT_ENABLED\)

tasks:
- install:
- ceph:
- workunit:
    clients:
      all:
        - cephtool/test_kvstore_tool.sh
